import 'package:flutter/material.dart';

class option2Screen extends StatelessWidget {
  const option2Screen({super.key, required history});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(
      appBar: AppBar(title: Text('options')),
      body: Column(
        children: [],
      ),
    ));
  }
}
