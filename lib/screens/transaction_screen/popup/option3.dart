import 'package:flutter/material.dart';

class option3Screen extends StatelessWidget {
  const option3Screen({super.key, required history});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
        child: Scaffold(
      appBar: AppBar(title: Text('options')),
      body: Column(
        children: [],
      ),
    ));
  }
}
